<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.geeksarena.afyayangu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.general.KeepingSafeActivity"></activity>
        <activity android:name=".views.general.AboutCorvid19Activity" />
        <activity android:name=".views.general.ContactTracingActivity" />
        <activity android:name=".views.general.MyMapViewActivity" />
        <activity android:name=".views.users.UserProfile" />
        <activity android:name=".views.general.LoginActivity" />
        <activity android:name=".views.general.DashboardActivity" />
        <activity android:name=".views.general.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>